# OPENCLAW_TASKS.md

## Project
Tectonic Super Bowl (MonoGame / SimArch football game)

## Branch / remote
- Branch: `main`
- Remote: `origin`

## Validation loop
- `dotnet build src/TecmoSB.sln`
- `dotnet run --project src/TecmoSBGame -- --headless-scrimmage-pack`
- For runtime/UI tasks, also run the game manually if needed: `dotnet run --project src/TecmoSBGame`

## Execute next
- [x] Validate the current working tree on `automation/validate-2026-05-18-tecmo-super-bowl-monogame` and commit/push if green. Scope: inspect the current uncommitted gameplay/audio/season-progress changes, run `dotnet build src/TecmoSB.sln`, `dotnet run --project src/TecmoSBGame -- --headless-scrimmage-pack`, and `dotnet run --project src/TecmoSBGame -- --save-roundtrip artifacts/save-roundtrip`, commit only if the tree is coherent and validation passes, then push and update this task with evidence. Acceptance: either (a) committed + pushed with validation evidence, or (b) task updated with the exact blocker preventing commit.
  - Done: inspected the working tree and found one coherent slice spanning gameplay/rules validation hardening, audio cue wiring/content, runtime observation artifacts, and season/save/meta scaffolding. Validation passed with `dotnet build src/TecmoSB.sln`, `dotnet run --project src/TecmoSBGame -- --headless-scrimmage-pack`, and `dotnet run --project src/TecmoSBGame -- --save-roundtrip artifacts/save-roundtrip` on 2026-05-18. Committed as `1f76e6d` (`Add runtime validation, audio, and season scaffolding`) and pushed to `origin/automation/validate-2026-05-18-tecmo-super-bowl-monogame`.
- [x] Build a concrete runtime-observation plan for autonomous Tecmo work. Scope: inspect the current launch/runtime path and document the smallest viable feedback loop that an agent can actually use from this environment (for example: scripted launch, deterministic scenario selection, screenshot/frame capture, and artifact output paths). Acceptance: add a concise repo doc or task note that names the exact commands, expected artifacts, known environment limits, and the next implementation step needed to make runtime behavior observable instead of code-only. Validation: verify the documented commands/paths against the current repo/runtime entry points; if a command cannot be run here, state the exact blocker honestly.
  - Done: added `docs/runtime-observation-loop.md` documenting the verified active entry points in `src/TecmoSBGame/Program.cs`, the current headless evidence loop, artifact locations under `artifacts/headless-determinism/`, the lack of an active screenshot/frame capture path in `MainGameArch`, and the concrete next step needed to make runtime/UI behavior observable. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --headless-scrimmage-pack`; `dotnet run --project src/TecmoSBGame -- --headless-determinism-check scrimmage-pack 2`.
- [x] Implement the first bounded runtime-feedback artifact path for Tecmo. Scope: add the smallest useful mechanism that lets a local run produce inspectable visual/runtime evidence for later review—prefer deterministic screenshot/frame/artifact capture tied to an existing scenario or launch path over broad UI rewrites. Acceptance: after one documented command, the repo produces durable artifacts that help compare what the game actually did between runs. Validation: `dotnet build src/TecmoSB.sln`; run the new capture path or the closest viable local harness and record artifact locations plus any honest environment limitations.
  - Done: added `--runtime-capture [ticks]` to `src/TecmoSBGame/Program.cs`/`MainGameArch.cs`, which boots the existing Arch runtime, advances a fixed deterministic tick count, saves `frame.png` plus `manifest.json` under `artifacts/runtime-captures/capture_<timestamp>/`, prints the artifact paths, and exits automatically. Verified with `dotnet run --project src/TecmoSBGame -- --runtime-capture 240`, which produced `artifacts/runtime-captures/capture_20260518_141843584/frame.png` (1280x1120 PNG, 56 KB) and `artifacts/runtime-captures/capture_20260518_141843584/manifest.json`. Honest limitation: this environment still cannot visually inspect or interact with the game window directly, so the first artifact path is a deterministic saved frame/metadata bundle rather than full interactive automation.
- [x] Add an agent/operator runbook for using Tecmo runtime evidence during future tasks. Scope: document how to launch the relevant scenario, where artifacts land, how to compare before/after results, and when a task should be considered blocked because observable feedback is still missing. Acceptance: a future coder agent can read one short doc and know how to get runtime evidence before making undirected gameplay/UI changes. Validation: review the runbook against the implemented capture path and repo layout for internal consistency.
  - Done: added `docs/runtime-evidence-runbook.md`, a short operator/agent runbook covering when to use `--headless-scrimmage-pack`, `--headless-determinism-check`, and `--runtime-capture 240`, where those artifacts land under `artifacts/headless-determinism/` and `artifacts/runtime-captures/`, how to compare before/after outputs, and when a Tecmo task should be treated as blocked because the current observable path does not reach the affected state. Validation: reviewed the runbook against `src/TecmoSBGame/Program.cs` and `src/TecmoSBGame/MainGameArch.cs`; `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --runtime-capture 240` (PASS, wrote `artifacts/runtime-captures/capture_20260518_144835924/frame.png` + `manifest.json`); `dotnet run --project src/TecmoSBGame -- --headless-determinism-check scrimmage-pack 2` (PASS, wrote `artifacts/headless-determinism/scrimmage-pack_20260518_144916046/`).
- [x] Perform a full code review of the current Tectonic Super Bowl working tree and assess how close the project is to a fully playable game. Acceptance: produce a concise evidence-based summary with code-review findings plus a playable-game readiness assessment. Evidence: reviewed current changed files + git status/log; validation passed with `dotnet build src/TecmoSB.sln` and `dotnet run --project src/TecmoSBGame -- --headless-2plays 240`.
- [x] Implement a minimal visible playcall/menu UI path for the Arch runtime that lets a human choose a play without relying on invisible or implicit input flow. Acceptance: a human can launch the game, see the play-selection state, make a choice, and start a play without guessing hidden controls.
- [x] Make offense/defense and player roles easier to verify visually in runtime by adding conservative debug cues without changing gameplay rules. Acceptance: during a manual run, a new developer can quickly distinguish teams and verify alignment responsibilities.
- [x] Audit controlled-player movement in the current Arch runtime and fix any remaining gaps between input capture and actual controlled-entity movement. Acceptance: the selected player responds deterministically to controls during the playable slice and does not silently drop movement intent.
- [x] Review the current Tectonic project hub and remove or demote stale metadata / workflow-only todos that should no longer block executable work. Acceptance: project memory remains intact, but non-executable notes are no longer shaped like actionable tasks.

### Ordered implementation queue

#### Phase 1 — core scrimmage correctness
- [x] 1. Fix pass-state bookkeeping so pass outcomes can be resolved authoritatively. Scope: persist pass metadata in `PassFlightStartSystem` and consume it in `PassFlightCompleteSystem`, so receiver/defender/targeting context survives into completion logic. Acceptance: pass completion logic can distinguish intended receiver, defender interaction, and dead-ball vs catch outcome without relying on implicit transient state. Validation: `dotnet build src/TecmoSB.sln`; add or update a headless passing scenario with assertions, not just tick advancement. Packet: `docs/phase1-agent-work-packets.md` (Packet A).
  - Done: authoritative pass state now lives on the ball flight itself (passer id, intended receiver id/role/slot, predicted target point, persisted primary defender snapshot). `--headless-pass-metadata` now asserts both targeted catch ownership and contested incompletion. Interceptions/returns still remain for task 2.
- [x] 2. Implement real incompletion, interception, and interception-return outcomes for SimArch passing. Scope: replace the current offense-catch-or-dead-ball behavior with explicit pass result branches and possession/state transitions. Acceptance: a deterministic passing scenario can end as completion, incompletion, or interception with correct ball ownership and play-end behavior. Validation: `dotnet build src/TecmoSB.sln`; run targeted headless passing scenarios covering all three outcomes. Packet: `docs/phase1-agent-work-packets.md` (Packet B).
  - Done: incomplete passes now emit authoritative play-end events, interceptions preserve live-ball possession through the return tackle, tackle resolution preserves the last carrier through rules resolution, and `--headless-pass-outcomes` covers deterministic completion/incompletion/interception scenarios (each run twice for repeatability).
- [x] 3. Harden QB AI, receiver progression, and coverage interaction around real pass outcomes. Scope: deepen `QbAiSystem`, coverage systems, and route/defender timing so pass results are driven by game state rather than loose heuristics. Acceptance: scripted pass scenarios behave consistently enough to exercise coverage wins, throws under pressure, and defender ball plays. Validation: `dotnet build src/TecmoSB.sln`; expand headless passing scenarios and verify repeat-run identical outcomes. Packet: `docs/phase1-agent-work-packets.md` (Packet C).
  - Done: pass resolution now keys defender leverage against the resolved target before contest evaluation, the interception harness pins defender ball-play at the actual pass endpoint, and the pressure-read harness deterministically forces the alternate receiver under immediate pressure. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --headless-pass-outcomes` twice (all scenarios PASS identically: Completion, Incomplete, Interception, CoverageBreakup, PressureThrow).
- [x] 4. Deepen blocking, rush, and engagement resolution beyond current heuristic scaffolding. Scope: improve block assignment fidelity, disengage behavior, pass-rush pressure, and double-team effects using play-data/disassembly guidance where available. Acceptance: line play materially affects run/pass outcomes and does not collapse into nearest-lane approximations only. Validation: `dotnet build src/TecmoSB.sln`; run existing scrimmage scenarios plus at least one pressure-focused headless case. Packet: `docs/phase1-agent-work-packets.md` (Packet D).
  - Done: blockers now track failed engagements and reassign pressure help more intelligently, second-level climbs wait for reachable second-level defenders, rushers attempt deterministic disengages from extended fights, and engagement duration/double-team pressure are tied to support/strength. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --headless-2plays 240`; `dotnet run --project src/TecmoSBGame -- --headless-pressure 120` (PASS: free pressure 59 ticks, single 22, double-team 14).
- [x] 5. Implement real tackle-driven fumble / loose-ball / recovery / turnover lifecycle. Scope: move fumbles out of debug-only behavior into authoritative play resolution and spotting rules. Acceptance: a forced fumble scenario can create a loose ball, allow recovery, update possession correctly, and reset the next play correctly. Validation: `dotnet build src/TecmoSB.sln`; add headless fumble and recovery scenarios with explicit assertions.
  - Done: tackle resolution now deterministically forces live loose-ball fumbles on qualifying tackle outcomes, loose-ball recovery feeds authoritative turnover/play-end handling, and `--headless-fumble` asserts both same-team recovery and defensive turnover/reset behavior. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --headless-fumble`.
- [x] 6. Harden play-end, spotting, first-down, touchdown, turnover, and reset behavior across repeated drives. Scope: verify `PlayLifecycleSystem`, `PlayEndSystem`, `DownDistanceSystem`, and reset systems over multi-play sequences instead of one-off tackles only. Acceptance: repeated drives can run without state corruption across run, pass, turnover, and scoring paths. Validation: `dotnet build src/TecmoSB.sln`; add a deterministic multi-play drive scenario with assertions for spot, down, distance, score, and possession.
  - Done: `PlayLifecycleSystem` now ignores duplicate `PlayEndedEvent`s for the same play id, and `--headless-drive` adds a deterministic repeated-drive scenario covering run gain, first down reset, touchdown scoring state, and turnover-driven possession/direction reset with asserted spot/down/distance/score/play-number checkpoints.

#### Phase 2 — rules and authoritative match state
- [x] 7. Fix match bookkeeping drift and make play numbering/state transitions authoritative. Scope: investigate and resolve the suspected double increment of `MatchState.PlayNumber` and any related play-id drift. Acceptance: one completed play advances counters exactly once across all normal play-end paths. Validation: `dotnet build src/TecmoSB.sln`; add assertions in a multi-play scenario for play-number monotonic correctness.
  - Done: removed the duplicate `MatchState.PlayNumber` increment from `SimArch/Systems/DownDistanceSystem`, leaving `PlayLifecycleSystem` authoritative for next-play ids/state transitions. `--headless-drive` now asserts exact +1 play-id progression and matching post-play `PlayNumber` checkpoints across run/first-down/touchdown/turnover flow. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --headless-2plays 240`; `dotnet run --project src/TecmoSBGame -- --headless-drive`.
- [x] 8. Complete clock semantics, quarter flow, halftime, side changes, and endgame flow. Scope: implement real stop/run behavior and game-phase transitions. Acceptance: a full-game simulation can move through quarter boundaries and halftime without desynchronizing possession, direction, or score state. Validation: `dotnet build src/TecmoSB.sln`; add headless quarter/halftime/endgame scenarios with explicit state assertions.
  - Done: SimArch match state now tracks match phase/clock-running semantics, quarter transitions flip field direction, halftime defers/starts second-half possession setup, and endgame marks a final state without extra phase drift. Added `--headless-quarter-flow` assertions covering Q1→Q2, halftime, Q3→Q4, and final game-over, while existing drive flow remains green. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --headless-quarter-flow`; `dotnet run --project src/TecmoSBGame -- --headless-drive`; `dotnet run --project src/TecmoSBGame -- --headless-2plays 240`.
- [x] 9. Complete authoritative scoreboard, field-position, turnover-on-downs, goal-to-go, and scoring integration. Scope: make score/spot/possession state the single source of truth across normal plays and scoring transitions. Acceptance: scoring and possession changes update HUD/match state correctly and survive into the next sequence. Validation: `dotnet build src/TecmoSB.sln`; run drive/scoring scenarios asserting score, possession, spot, and next-state setup.
  - Done: SimArch match state now tracks authoritative goal-to-go/field-position state, turnovers on downs flip possession from the end spot, touchdowns/safeties reset the next sequence spot consistently, and the HUD snapshot renders possession plus goal-to-go aware down/distance. Added `--headless-scoreboard` coverage for goal-line short, turnover-on-downs, and touchdown carry-forward, while keeping `--headless-drive` green against the updated spotting logic. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --headless-drive`; `dotnet run --project src/TecmoSBGame -- --headless-scoreboard`.
- [x] 10. Port per-play and per-game stats accumulation plus minimal replay/event capture needed for validation. Scope: begin the SimArch equivalent of the original bookkeeping/stats layer and wire enough event capture to inspect outcomes. Acceptance: core offensive/defensive/kicking events produce structured state updates that can be asserted in tests and later surfaced in UI/season systems. Validation: `dotnet build src/TecmoSB.sln`; add assertions for at least one passing, rushing, and turnover stat update path.
  - Done: added initial `StatsState`/`StatsSystem` bookkeeping for per-play + per-game rushing, passing, and turnover accumulation, plus replay event capture entries on `ReplayCapture`. Added `--headless-stats` focused assertions covering passing completion, rushing gain, and interception turnover paths. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --headless-stats`.

#### Phase 3 — special teams and score transitions
- [x] 11. Port score-to-kickoff orchestration and `KickoffAfterScoreSystem` as the special-teams backbone. Scope: wire deterministic score → kickoff setup events before trying to finish all special teams. Acceptance: after a scoring event, the game can enter a valid kickoff setup state without manual glue. Validation: `dotnet build src/TecmoSB.sln`; add a forced-score headless scenario that asserts kickoff setup state.
  - Done: added SimArch `KickoffAfterScoreSystem` wiring through `PlayLifecycleSystem`, persisted pending kickoff metadata on `MatchState`, and added `--headless-kickoff-after-score` to assert touchdown → kickoff setup event/state without manual glue. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --headless-kickoff-after-score`; `dotnet run --project src/TecmoSBGame -- --headless-scoreboard`.
- [x] 12. Implement a playable kickoff path including flight, catch/recovery, return, touchback, and post-kickoff possession setup. Acceptance: kickoff sequences can start and end a possession correctly. Validation: `dotnet build src/TecmoSB.sln`; add headless kickoff scenarios for normal return and touchback, plus manual run if visual confirmation helps.
  - Done: added SimArch kickoff setup/flight/return orchestration with basic coverage + return steering, catch/loose recovery handling, touchback resolution, and kickoff-specific possession/bootstrap rules. Added `--headless-kickoff` coverage for deterministic return and touchback outcomes while keeping `--headless-kickoff-after-score` green. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --headless-kickoff-after-score`; `dotnet run --project src/TecmoSBGame -- --headless-kickoff`.
- [x] 13. Implement a playable punt path including snap, kick, coverage, return, downing, muff, and touchback handling. Acceptance: punts are a real possession-transition tool, not parser-only scaffolding. Validation: `dotnet build src/TecmoSB.sln`; add headless punt scenarios for standard return and touchback/downed outcomes.
  - Done: added SimArch punt orchestration with punt flight bootstrap, coverage/return steering, downed-ball and touchback resolution, plus muff handling through the live loose-ball path. Added `--headless-punt` coverage for deterministic standard return, touchback, downed, and muff outcomes. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --headless-punt`.
- [x] 14. Implement field goal / extra point gameplay flow and integrate results into authoritative score state. Acceptance: FG/PAT attempts can succeed, fail, or be blocked with correct score and continuation behavior. Validation: `dotnet build src/TecmoSB.sln`; add headless FG/PAT scenarios plus one manual check if rendering/UX changed.
  - Done: field goals/PATs now run through a live kick-flight path, good kicks queue kickoff state, misses turn over at the hold spot, and blocked PATs continue from the recovery spot with flipped possession. Added `--headless-field-goal` coverage for good FG, missed FG, blocked PAT, and good PAT outcomes. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --headless-field-goal`. Manual run skipped because rendering/UX did not change.

#### Phase 4 — validation hardening (must grow in parallel with phases 1–3)
- [x] 15. Replace the current 2-play smoke test as the main confidence gate with named asserted scrimmage scenarios. Scope: add regression scenarios for run, pass, turnover, scoring, and reset flows. Acceptance: the primary validation signal is explicit asserted football-state correctness, not just “ticks advanced.” Validation: `dotnet build src/TecmoSB.sln`; run the expanded headless scenario pack and capture passing output.
  - Done: added `--headless-scrimmage-pack` as the primary confidence gate, chaining named asserted run/pass/turnover/scoring/reset scrimmage validations (`headless-drive`, `headless-pass-outcomes`, `headless-fumble`, `headless-scoreboard`, `headless-quarter-flow`) instead of relying on the old tick-advance smoke test. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --headless-scrimmage-pack`.
- [x] 16. Add repeat-run determinism checks and begin meaningful replay/trace artifacts. Scope: verify the same seed/scenario yields the same result and start capturing structured replay/event output for diffs. Acceptance: core scenarios can be run repeatedly with identical outputs, and failures are inspectable. Validation: `dotnet build src/TecmoSB.sln`; run the same named scenario multiple times and compare outputs automatically.
  - Done: added `--headless-determinism-check <scenario> [runs]`, which emits structured JSON artifacts under `artifacts/headless-determinism/`, compares repeated runs byte-for-byte, and reports artifact paths plus the first diff on failure. Added artifact builders for scrimmage-pack, pass, fumble, scoreboard, quarter-flow, kickoff, punt, field-goal, pressure, and stats traces/replay-event payloads. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --headless-determinism-check scrimmage-pack 3`; `dotnet run --project src/TecmoSBGame -- --headless-determinism-check stats 3`.

#### Phase 5 — launch-to-exhibition UX
- [x] 17. Wire the existing front-end renderers and flow controller into `MainGameArch` for a coherent launch → exhibition path. Scope: connect title, main menu, team select, coin toss, playcall, HUD, and post-play flow using the existing renderers before attempting a bigger UI rewrite. Acceptance: a player can launch the game and reach a playable exhibition flow without relying on debug knowledge. Validation: `dotnet build src/TecmoSB.sln`; manual run from launch through kickoff/scrimmage start; record a brief checklist of screens visited.
  - Done: `MainGameArch` now owns the SimArch `GameFlowController`, routes title/main-menu/team-select/coin-toss/kickoff/on-field/post-play states through the existing renderers, syncs post-play transitions back from SimArch lifecycle state, and shows the HUD/playcall/post-play overlays during field flow. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --headless-2plays 240`; best-effort local runtime launch confirmed startup path reaches the new front-end flow host, but full interactive screen-by-screen walkthrough was not feasible in this environment because the game window cannot be observed/driven here and startup emits the existing YAML validation flood to stdout.
- [x] 18. Add pause and upgrade the active HUD/post-play presentation from debug-first to player-readable. Scope: keep the minimal playcall overlay, defer Gum, and improve score strip, pause behavior, and post-play messaging. Acceptance: the runtime can be paused safely and game state is understandable during and after plays. Validation: `dotnet build src/TecmoSB.sln`; manual run verifying pause/resume, HUD readability, and post-play summary behavior.
  - Done: added a safe `P`-toggle pause gate in `MainGameArch`/`Sim`, replaced the tiny debug HUD with a readable overlay showing possession/situation/status/last-play summary, and expanded the post-play card with clearer metadata and explanatory messaging while keeping the minimal playcall overlay. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --headless-2plays 240`. Manual interactive verification remains blocked in this environment because the game window cannot be observed or driven here.

#### Phase 6 — audio/content baseline
- [x] 19. Add a SimArch audio bridge and the minimum viable asset/content pipeline for gameplay/menu cues. Scope: wire the active runtime to sound playback and check in the minimum real assets needed for snap, whistle, tackle, incomplete, turnover, touchdown, and menu feedback. Acceptance: the active runtime is no longer silent and missing cues are explicit rather than implicit. Validation: `dotnet build src/TecmoSB.sln`; manual run confirming the targeted cues fire in the right moments; document any remaining placeholder/missing content.
  - Done: added `SimArchAudioBridge` to drain SimArch snap/whistle/play-end/play-selection events into runtime cues, wired `MainGameArch` menu navigation + field flow into the new audio bridge, enabled MGCB audio builds, and checked in a minimal generated WAV set for snap/whistle/tackle/incomplete/turnover/touchdown/menu move/menu select. Missing cues now surface in the live HUD instead of failing silently. Validation: `dotnet build src/TecmoSB.sln` (PASS). Manual runtime confirmation was not possible in this environment because the game window/audio device cannot be observed from here; remaining content is placeholder synthesized audio rather than final/authentic assets.

#### Phase 7 — season/save/meta after exhibition is real
- [x] 20. Implement persistence skeleton: `SaveManager`, save-path resolver, versioned JSON envelopes, and one working settings/season-slot round trip. Acceptance: the game can create, write, and reload a minimal settings or season artifact without manual editing. Validation: `dotnet build src/TecmoSB.sln`; run a local save/load round trip and verify the saved artifact on disk.
  - Done: added `Persistence/SaveManager`, `SavePathResolver`, versioned `SaveEnvelope<T>` models, and a `--save-roundtrip [rootDir]` harness that writes and reloads both settings and a minimal season slot. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --save-roundtrip artifacts/save-roundtrip` (PASS, wrote `artifacts/save-roundtrip/settings/settings.json` and `artifacts/save-roundtrip/seasons/season-1.json`).
- [x] 21. Implement season domain scaffolding: schedule/standings model, sim-result contract, and basic progression hooks. Acceptance: exhibition-complete gameplay can feed a minimal season state model without inventing a second bookkeeping path later. Validation: `dotnet build src/TecmoSB.sln`; add a focused test or runnable harness that advances at least one simulated week deterministically.
  - Done: added persistent season-state scaffolding (`SeasonState`, weekly schedule/standings models, `SeasonGameResult` contract, `SeasonManager`, and save/load support) plus a `--season-roundtrip [rootDir]` harness that deterministically creates a 4-team round-robin season, simulates week 1, updates standings, and reloads the saved season state/slot artifacts. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --season-roundtrip artifacts/season-roundtrip`.
- [x] 22. Implement leaders/records/playoffs/Pro Bowl/static season UI in dependency order. Acceptance: a season can be progressed, summarized, saved, resumed, and surfaced through basic meta screens. Validation: `dotnet build src/TecmoSB.sln`; run a deterministic season progression flow and manually verify the new screens/state.
  - Done: expanded `SeasonState` with derived leaders/records/playoff/Pro Bowl summaries, added `SeasonPresentationService` plus `--season-meta-flow` deterministic save/resume harness, and wired a text-first `SeasonMeta` front-end screen path off the main menu so season state can be surfaced in-game via hub/standings/leaders/records/playoffs/Pro Bowl pages. Validation: `dotnet build src/TecmoSB.sln`; `dotnet run --project src/TecmoSBGame -- --season-meta-flow artifacts/season-meta-flow` (PASS, wrote `artifacts/season-meta-flow/season-meta-summary.txt`). Manual screen verification remains best-effort only in this environment because the game window cannot be observed/driven here.

## Blocked
- Gum-heavy UI work that depends on assets or a broader final UI decision can be deferred if a smaller demo overlay completes the same immediate need.

## Context files
- Project hub directory: `/home/montblanc/.openclaw/workspace/Projects/tectonic-super-bowl-clone/`
- Agent context: `/home/montblanc/.openclaw/workspace/Projects/tectonic-super-bowl-clone/context.md`

## Project hub notes
- The workspace hub currently serves as reference memory only (`context.md` plus reverse-engineering notes); it is not an executable task queue.
- The earlier `project.md` summary reference is stale and has been removed because that file is not present.
